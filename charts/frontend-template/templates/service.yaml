apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-template.fullname" . }}
  labels:
    {{- include "frontend-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontend-template.selectorLabels" . | nindent 4 }}
